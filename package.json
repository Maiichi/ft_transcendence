{
  "name": "root",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/Maiichi/ft_transcendence.git",
  "author": "ibouroum",
  "license": "MIT",
  "devDependencies": {
    "husky": "^7.0.4"
  },
  "scripts": {
    "prepareStart": "rm -rf node_modules && yarn && yarn prepare && chmod ug+x .husky/*",
    "prepare": "husky install"
  },
  "dependencies": {
    "@commitlint/cli": "^16.0.1",
    "@commitlint/config-conventional": "^16.0.0"
  }
}
